[package]
name = "knave-shell-core"
version = "0.1.0"
edition = "2024"
description = "Typed state and compositor services for Knave Shell"
license = "MIT"

[lib]
crate-type = ["rlib", "staticlib"]

[dependencies]
base64 = "0.22"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

